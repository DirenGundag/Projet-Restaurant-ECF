{% extends 'base.html.twig' %}

{% block title %}Formule{% endblock %}

{% block content %}
<div class="container">
    <h1>Formule</h1>

    <table class="table">
        <tbody>
            {# <tr>
                <th>Id</th>
                <td>{{ formule.id }}</td>
            </tr> #}
            <tr>
                <th>Description</th>
                <td>{{ formule.description }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ formule.price }}</td>
            </tr>
        </tbody>
    </table>

    <a class="btn" href="{{ path('app_formule_index') }}">Précédent</a>

    <a class="btn" href="{{ path('app_formule_edit', {'id': formule.id}) }}">Modifier</a>

    {{ include('formule/_delete_form.html.twig') }}
</div>
    <style>
    .btn {
        background-color:B87333;
        color:FFFFFF;
        margin-top:2%;
    }
     a:hover {
  color:000000;
}
h1 {
    font-family: Arial;
    text-align: center;
}
</style>
{% endblock %}
