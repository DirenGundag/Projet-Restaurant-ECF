<form method="post" action="{{ path('app_menu_delete', {'id': menu.id}) }}" onsubmit="return confirm('Etes vous sûr de supprimer ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ menu.id) }}">
    <button class="btn">Supprimer</button>
</form>
<style>
    .btn {
        background-color:B87333;
        color:FFFFFF;
       
    }
     a:hover {
  color:000000;
}
</style>