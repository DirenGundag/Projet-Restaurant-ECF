<form method="post" action="{{ path('app_seuil_maximum_delete', {'id': seuil_maximum.id}) }}" onsubmit="return confirm('Etes vous sûr de supprimer ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ seuil_maximum.id) }}">
    <button class="btn">Supprimer</button>
</form>
